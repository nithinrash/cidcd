apiVersion: v1
kind: Service
metadata:
  name: backend-external
spec:
  type: LoadBalancer
  selector:
    app: fastapi-backend  # Make sure this matches your backend pod labels
  ports:
  - port: 80
    targetPort: 80